<%= render @user %>
<div class="my-2">
<% if @user.bio.present?%>
	<div class="mb-5 notification">
	  <h5 class="title is-6">Bio</h5>
	  <p class="is-6"><%= @user.bio %></p>
	</div>
<% end %>
</div>
<h2 class="title is-5">Posts</h2>
<%= render partial: "posts/post", collection: @posts %>
<div class="is-align-items-center">
  <%= link_to "See all post", user_posts_path(@user.id), class: "button is-link is-small" %>
</div>